import React from "react";

const Card = props => {
    return (
        <div>
            <h3>{props.blok.title}</h3>
            <p>This is card component</p>
        </div>
    );
};

export default Card;
